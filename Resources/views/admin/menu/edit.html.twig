{# @TODO : Use the configured layout #}
{% extends "@UmanitTree/admin/default_layout.html.twig" %}

{% form_theme form ':admin:menu/menu_form_layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-12">
                    <div class="box box-primary">
                        <div class="box-header">
                            <h4 class="box-title">
                                Menu
                            </h4>
                        </div>
                        <div class="box-body">
                            {{ form(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{%  endblock %}
